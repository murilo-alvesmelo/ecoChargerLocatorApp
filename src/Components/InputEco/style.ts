import {StyleSheet} from 'react-native';
import colors from '../../utils/colors';

const style = StyleSheet.create({
  input: {
    fontSize: 16,
    borderWidth: 1,
    borderColor: colors.brancoForma,
    borderRadius: 8,
    padding: 12,
    color: colors.brancoForma,
    marginVertical: 8,
  },
});

export default style;
